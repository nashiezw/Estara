import assert from "node:assert/strict";
import test from "node:test";
async function render(path="/"){const u=new URL("../dist/server/index.js",import.meta.url);u.searchParams.set("test",`${process.pid}-${Date.now()}-${path}`);const{default:w}=await import(u.href);return w.fetch(new Request(`http://localhost${path}`,{headers:{accept:"text/html"}}),{ASSETS:{fetch:async()=>new Response("Not found",{status:404})}},{waitUntil(){},passThroughOnException(){}})}
test("server-renders the ESTARA landing and public auth entry points",async()=>{const [landing,login]=await Promise.all([render(),render("/login")]);for(const response of[landing,login]){assert.equal(response.status,200);assert.match(response.headers.get("content-type")??"",/^text\/html\b/i)}const landingHtml=await landing.text(),loginHtml=await login.text();assert.match(landingHtml,/<title>ESTARA/);assert.match(landingHtml,/real estate operating system/i);assert.match(landingHtml,/Zimbabwe-first/);assert.match(landingHtml,/Start your agency setup/);assert.match(loginHtml,/Secure login/);assert.match(loginHtml,/Email/);assert.match(loginHtml,/Password/);assert.doesNotMatch(landingHtml+loginHtml,/codex-preview|Your site is taking shape|react-loading-skeleton/i)});
