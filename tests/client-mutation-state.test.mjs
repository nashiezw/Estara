import assert from"node:assert/strict";import{readFile}from"node:fs/promises";import test from"node:test";const read=p=>readFile(new URL(p,import.meta.url),"utf8");
test("public client mutations recover from failed requests with retryable state",async()=>{const[client,checklist]=await Promise.all([read("../app/site/[slug]/public-client.tsx"),read("../docs/DELIVERY-CHECKLIST.md")]);assert.match(client,/try\{const startsAt/);assert.match(client,/catch\{setState\("error"\)\}/);assert.match(client,/role="alert"/);assert.match(client,/state==="error"\?"Retry request"/);assert.match(client,/disabled=\{state==="sending"\}/);assert.match(checklist,/client-mutation state audit complete/)});
