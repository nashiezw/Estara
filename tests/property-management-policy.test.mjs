import assert from "node:assert/strict";import test from "node:test";import{allocateOldest,dueDate,leaseDates,managementFee,minor,rentPeriod}from"../db/property-management-policy.ts";
test("money is stored as exact integer minor units",()=>{assert.equal(minor("1200.45"),120045);assert.equal(managementFee(10001,800),800);assert.throws(()=>minor(0),/positive/);assert.throws(()=>managementFee(100,10001),/Invalid/)});
test("payments allocate oldest debt first and preserve credit",()=>{const result=allocateOldest(17000,[{id:"jan",amountMinor:10000,allocatedMinor:2000},{id:"feb",amountMinor:6000,allocatedMinor:0}]);assert.deepEqual(result,{allocations:[{chargeId:"jan",amountMinor:8000},{chargeId:"feb",amountMinor:6000}],unallocatedMinor:3000});assert.equal(result.allocations.reduce((sum,row)=>sum+row.amountMinor,0)+result.unallocatedMinor,17000)});
test("rent periods, due days and lease windows are deterministic",()=>{assert.equal(rentPeriod("2026-02"),"2026-02");assert.equal(dueDate("2026-02",31),"2026-02-28T12:00:00.000Z");assert.equal(leaseDates("2026-01-01","2026-12-31").startsAt,"2026-01-01T00:00:00.000Z");assert.throws(()=>rentPeriod("Feb 2026"),/YYYY-MM/);assert.throws(()=>leaseDates("2026-12-31","2026-01-01"),/invalid/)});
