import assert from"node:assert/strict";import test from"node:test";import{conditionsMatch,renderTemplate,retryDelayMs,validateActions}from"../db/automation-policy.ts";
test("automation conditions are deterministic and fail closed",()=>{assert.equal(conditionsMatch({stage:"New",roles:["buyer"]},[{field:"stage",operator:"equals",value:"New"},{field:"roles",operator:"includes",value:"buyer"}]),true);assert.equal(conditionsMatch({stage:"New"},[{field:"missing",operator:"exists"}]),false)});
test("templates use event facts without evaluating code",()=>{assert.equal(renderTemplate("Respond to {{name}} about {{property}}",{name:"Tariro",property:"12 Lane"}),"Respond to Tariro about 12 Lane");assert.equal(renderTemplate("{{constructor}}",{}),"")});
test("actions are allowlisted and retries use bounded backoff",()=>{assert.equal(validateActions([{type:"notification",recipientField:"assignedUserId"},{type:"webhook",recipientField:"x"}]).length,1);assert.equal(retryDelayMs(1),60000);assert.ok(retryDelayMs(20)<=21600000)});
