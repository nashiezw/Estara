import assert from"node:assert/strict";import{readFile}from"node:fs/promises";import test from"node:test";const read=p=>readFile(new URL(p,import.meta.url),"utf8");
test("property capture uses real files, cross-reload queue recovery and bounded upload retries",async()=>{const[source,route]=await Promise.all([read("../app/estara-app.tsx"),read("../app/api/workspace/route.ts")]);assert.match(source,/estara-property-draft/);assert.match(source,/indexedDB\.open\("estara-capture-queue",1\)/);assert.match(source,/fileQueueReady/);assert.match(source,/if\(fileQueueReady\)saveCaptureFiles\(files\)/);assert.match(source,/loadCaptureFiles\(\)\.then/);assert.match(source,/clearCaptureFiles\(\)/);assert.match(source,/beforeunload/);assert.match(source,/type="file"[\s\S]*multiple/);assert.doesNotMatch(source,/simulate phone uploads|type="range" min="0" max="20"/);assert.match(source,/attempt<=3/);assert.match(source,/Draft could not be saved/);assert.match(source,/localStorage\.removeItem\("estara-property-draft"\)/);assert.match(route,/photos=0/);assert.doesNotMatch(route,/photos=Math\.max\(0,Math\.round\(Number\(b\.photos\)\)\)/)});
test("low-data mode is persistent and prevents listing image requests",async()=>{const[source,css]=await Promise.all([read("../app/estara-app.tsx"),read("../app/reliability.css")]);assert.match(source,/estara-low-data/);assert.match(source,/lowData\?undefined:selected\.media/);assert.match(source,/!lowData&&p\.media/);assert.match(source,/loading="lazy"/);assert.match(css,/\.low-data \.photo/);assert.match(css,/background-image:none!important/);assert.match(css,/prefers-reduced-motion/);assert.match(css,/:focus-visible/)});
