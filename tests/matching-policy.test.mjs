import assert from"node:assert/strict";import test from"node:test";import{explainMatch,stringList}from"../db/matching-policy.ts";
const requirement={transactionType:"Sale",propertyTypes:["House"],locations:["Borrowdale"],minPriceMinor:15000000,maxPriceMinor:30000000,minBedrooms:3,minBathrooms:2,features:["solar","borehole"]};
test("matching is deterministic, explainable and bounded",()=>{const property={transactionType:"Sale",propertyType:"House",priceMinor:24500000,bedrooms:4,bathrooms:3,location:"Borrowdale, Harare",features:["solar","borehole","garden"],status:"Available"},first=explainMatch(requirement,property),second=explainMatch(requirement,property);assert.deepEqual(first,second);assert.equal(first.score,100);assert.ok(first.reasons.includes("Within budget"));assert.ok(first.reasons.some(x=>x.startsWith("Features:")))});
test("closed and wrong-journey listings fail closed",()=>{const base={transactionType:"Rent",propertyType:"House",priceMinor:24500000,bedrooms:4,bathrooms:3,location:"Borrowdale",features:[],status:"Available"};assert.equal(explainMatch(requirement,base).score,0);assert.equal(explainMatch(requirement,{...base,transactionType:"Sale",status:"Sold"}).score,0)});
test("requirement lists are normalized, deduplicated and bounded",()=>{assert.deepEqual(stringList([" Borrowdale ","Borrowdale","Newlands"]),["Borrowdale","Newlands"]);assert.equal(stringList(Array.from({length:30},(_,i)=>String(i))).length,12)});
