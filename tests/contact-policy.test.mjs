import assert from "node:assert/strict";import test from "node:test";import{canTransition,normalizeEmail,normalizePhone,normalizeRoles}from"../db/contact-policy.ts";
test("Zimbabwe phone and email normalization is deterministic",()=>{assert.equal(normalizePhone("0772 123 456"),"+263772123456");assert.equal(normalizePhone("263 772 123 456"),"+263772123456");assert.equal(normalizePhone("invalid"),"");assert.equal(normalizeEmail(" Person@Example.COM "),"person@example.com");assert.equal(normalizeEmail("not-an-email"),"")});
test("contact roles are allowlisted and deduplicated",()=>{assert.deepEqual(normalizeRoles(["buyer","buyer","LANDLORD","root"]),["buyer","landlord"]);assert.deepEqual(normalizeRoles([]),["buyer"])});
test("enquiry stage transitions reject skips and reopening closed work",()=>{assert.equal(canTransition("New","Contacted"),true);assert.equal(canTransition("New","Won"),false);assert.equal(canTransition("Offer","Won"),true);assert.equal(canTransition("Closed","New"),false);assert.equal(canTransition("Unknown","New"),false)});