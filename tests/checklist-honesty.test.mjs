import assert from"node:assert/strict";import{readFile}from"node:fs/promises";import test from"node:test";
test("delivery docs do not report incomplete or simulated work as complete",async()=>{const[checklist,todo]=await Promise.all([readFile(new URL("../docs/DELIVERY-CHECKLIST.md",import.meta.url),"utf8"),readFile(new URL("../docs/PRODUCTION-READINESS-TODO.md",import.meta.url),"utf8")]),completed=checklist.split(/\r?\n/).filter(line=>/^\s*- \[x\]/.test(line)),misstated=completed.filter(line=>!/No incomplete or simulated feature/.test(line)&&/\b(pending|not implemented|simulated|placeholder|fake|approval pending|provider pending)\b/i.test(line));assert.deepEqual(misstated,[]);assert.match(checklist,/- \[-\] .*pending/i);assert.match(todo,/do not claim provider payments complete until verified/i)});
