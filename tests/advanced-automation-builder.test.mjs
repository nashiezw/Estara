import assert from"node:assert/strict";import{readFile}from"node:fs/promises";import test from"node:test";const read=p=>readFile(new URL(p,import.meta.url),"utf8");test("advanced builder exposes triggers, conditions, actions and approvals",async()=>{const ui=await read("../app/operations/operations-client.tsx");assert.match(ui,/TRIGGER → CONDITIONS → ACTION/);assert.match(ui,/conditionOperator/);assert.match(ui,/next_action/);assert.match(ui,/dueMinutes/);assert.match(ui,/human approval/);assert.match(ui,/immutable rule/)});test("automation policy allowlists fields, operators, actions and bounded delays",async()=>{const policy=await read("../db/automation-policy.ts");assert.match(policy,/validateConditions/);assert.match(policy,/equals.*not_equals.*includes.*exists/);assert.match(policy,/notification.*next_action/);assert.match(policy,/525600/);assert.match(policy,/slice\(0,10\)/g)});
