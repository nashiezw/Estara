"use client";import{useEffect}from"react";import{DEFAULT_PLATFORM_IDENTITY}from"../db/platform-defaults";export default function ErrorBoundary({error,reset}:{error:Error&{digest?:string};reset:()=>void}){useEffect(()=>{console.error(JSON.stringify({event:"ui.error_boundary",digest:error.digest||null,message:error.message.slice(0,300)}))},[error]);return <main className="global-state" role="alert"><span>{DEFAULT_PLATFORM_IDENTITY.shortName}</span><h1>This page could not be completed.</h1><p>Your saved records are unchanged. Retry the page, or return to the workspace and continue safely.</p><div><button onClick={reset}>Retry now</button><a href="/workspace">Return to workspace</a></div>{error.digest&&<small>Support reference: {error.digest}</small>}</main>}
