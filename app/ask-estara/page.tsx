import{requireChatGPTUser}from"../chatgpt-auth";import{getPlatformIdentity}from"../../db/platform-settings";import AskClient from"./ask-client";import"../management/management.css";export const dynamic="force-dynamic";export default async function Page(){await requireChatGPTUser("/ask-estara");const platform=await getPlatformIdentity();return <AskClient platform={{shortName:platform.shortName}}/>}
