"use client";import{useEffect,useState}from"react";export default function InviteClient(){const[state,setState]=useState("Accepting your invitation...");useEffect(()=>{const token=new URLSearchParams(location.search).get("token")||"";fetch("/api/team/accept",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({token})}).then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.error);setState("Invitation accepted. Your agency workspace is ready.")}).catch(e=>setState(e.message||"This invitation could not be accepted."))},[]);return <main className="onboarding"><section className="onboarding-card"><div className="brand"><i>E</i><span>ESTARA<small>Team invitation</small></span></div><h1>{state}</h1><p>ESTARA verifies the signed-in email before granting agency access.</p><a className="primary invite-link" href="/">Open workspace</a></section></main>}