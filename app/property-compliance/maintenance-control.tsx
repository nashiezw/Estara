"use client";import{FormEvent,useEffect,useState}from"react";type Row=Record<string,any>;export default function MaintenanceControl(){const[jobs,setJobs]=useState<Row[]>([]),[contractors,setContractors]=useState<Row[]>([]),[message,setMessage]=useState("");const load=()=>fetch("/api/property-operations").then(r=>r.json()).then(x=>{setJobs(x.requests||[]);setContractors(x.contractors||[])});useEffect(()=>{load()},[]);const save=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const response=await fetch("/api/property-operations",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"maintenance_update",...Object.fromEntries(new FormData(e.currentTarget))})}),body=await response.json();setMessage(response.ok?"Maintenance financials and assignment updated.":body.error);if(response.ok)load()};return <section className="pm-shell" style={{minHeight:"auto",paddingTop:0}}><div className="pm-panel pm-wide"><p className="pm-kicker">MAINTENANCE CONTROL</p><h2>Quote, approval, scheduling & actual cost</h2><form onSubmit={save}><label>Request<select name="id" required>{jobs.map(x=><option value={x.id} key={x.id}>{x.property} · {x.title}</option>)}</select></label><div className="pm-two"><label>Status<select name="status"><option value="triaged">Reported / triaged</option><option value="awaiting_approval">Awaiting approval</option><option value="approved">Approved / assigned</option><option value="scheduled">Scheduled</option><option value="in_progress">In progress</option><option value="completed">Completed</option><option value="cancelled">Cancelled</option></select></label><label>Contractor<select name="contractorId"><option value="">Unassigned</option>{contractors.map(x=><option value={x.id} key={x.id}>{x.fullName} · {x.trade}</option>)}</select></label></div><div className="pm-two"><label>Estimated quote<input name="estimated" type="number" min="0" step="0.01"/></label><label>Approved cost<input name="approved" type="number" min="0" step="0.01"/></label></div><div className="pm-two"><label>Actual cost<input name="actual" type="number" min="0" step="0.01"/></label><label>Scheduled<input name="scheduledAt" type="datetime-local"/></label></div><label>Audited update note<input name="note" required/></label><button className="pm-primary">Save controlled update</button>{message&&<p>{message}</p>}</form></div></section>}
