"use client";import{useEffect,useState}from"react";type Row=Record<string,any>;export default function PortalAccessControl(){const[rows,setRows]=useState<Row[]>([]),[message,setMessage]=useState("");const load=()=>fetch("/api/property-operations").then(r=>r.json()).then(x=>setRows(x.grants||[]));useEffect(()=>{load()},[]);const revoke=async(id:string)=>{const response=await fetch("/api/property-operations",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"revoke_portal",id})});setMessage(response.ok?"Access revoked immediately.":"Access could not be revoked.");load()};return <section className="pm-shell" style={{minHeight:"auto",paddingTop:0}}><div className="pm-panel pm-wide"><p className="pm-kicker">PORTAL ACCESS</p><h2>Active and invited access</h2>{message&&<div className="pm-alert">{message}</div>}{rows.map(x=><div className="pm-row" key={x.id}><span><strong>{x.email}</strong><small>{x.property} · {x.audience} · {x.acceptedAt?"accepted":"invited"}</small></span>{!x.revokedAt&&<button className="pm-secondary" onClick={()=>revoke(x.id)}>Revoke</button>}</div>)}</div></section>}
