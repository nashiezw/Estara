import{requireChatGPTUser}from"../chatgpt-auth";import{getPlatformIdentity}from"../../db/platform-settings";import IntegrationsClient from"./integrations-client";import"../management/management.css";export const dynamic="force-dynamic";export default async function Page(){await requireChatGPTUser("/integrations");const platform=await getPlatformIdentity();return <IntegrationsClient platform={{shortName:platform.shortName}}/>}
