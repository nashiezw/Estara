import{requireChatGPTUser}from"../chatgpt-auth";import{getPlatformIdentity}from"../../db/platform-settings";import PlatformToolHeader from"../components/PlatformToolHeader";import IntegrationsClient from"./integrations-client";import"../management/management.css";export const dynamic="force-dynamic";export default async function Page(){await requireChatGPTUser("/integrations");const platform=await getPlatformIdentity(),brand={shortName:platform.shortName,platformName:platform.platformName,logoUrl:platform.logoUrl,iconUrl:platform.iconUrl,primaryColor:platform.primaryColor,accentColor:platform.accentColor};return <main className="pm-shell"><PlatformToolHeader platform={brand} section="Integrations"/><IntegrationsClient platform={brand}/></main>}
