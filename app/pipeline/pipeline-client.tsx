"use client";import{useEffect,useMemo,useState}from"react";const stages=["New","Contacted","Qualified","Viewing","Offer","Won","Lost","Closed"];export default function PipelineClient(){const[rows,setRows]=useState<any[]>([]),[query,setQuery]=useState(""),[message,setMessage]=useState("");const load=()=>fetch("/api/workspace").then(r=>r.json()).then(j=>setRows(j.enquiries||[]));useEffect(()=>{load()},[]);const filtered=useMemo(()=>rows.filter(x=>`${x.name} ${x.property} ${x.phone||""}`.toLowerCase().includes(query.toLowerCase())),[rows,query]);async function move(row:any,stage:string){const r=await fetch("/api/workspace/actions",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"transition_enquiry",resourceId:row.id,stage})}),j=await r.json();setMessage(r.ok?`${row.name} moved to ${stage}.`:j.error);if(r.ok)load()}return <main className="pipeline-page"><nav><a href="/">← Workspace</a><strong>ESTARA <small>Conversion pipeline</small></strong><a href="/contacts">Contacts →</a></nav><header><div><span>OPPORTUNITIES IN MOTION</span><h1>Enquiry pipeline</h1><p>Every lead has a visible stage, responsible agent and next move.</p></div><input aria-label="Search pipeline" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search client or property…"/></header>{message&&<div className="pipeline-message" role="status">{message}</div>}<section className="pipeline-board">{stages.map(stage=><article key={stage}><header><strong>{stage}</strong><b>{filtered.filter(x=>(x.stage||x.status)===stage).length}</b></header>{filtered.filter(x=>(x.stage||x.status)===stage).map(x=><section key={x.id}><small>{x.property}</small><h2>{x.name}</h2><p>{x.phone||x.email||"No contact detail"}</p><span>{x.assignedEmail||"Unassigned"}</span><label>Move to<select aria-label={`Move ${x.name} to stage`} value={stage} onChange={e=>move(x,e.target.value)}><option>{stage}</option>{stages.filter(s=>s!==stage).map(s=><option key={s}>{s}</option>)}</select></label></section>)}</article>)}</section></main>}
