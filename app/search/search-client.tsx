"use client";import{useEffect,useState}from"react";export default function SearchClient(){const[q,setQ]=useState(""),[items,setItems]=useState<any[]>([]),[busy,setBusy]=useState(false);useEffect(()=>{const t=setTimeout(async()=>{if(q.trim().length<2){setItems([]);return}setBusy(true);const r=await fetch(`/api/search?q=${encodeURIComponent(q)}`),j=await r.json();setItems(j.results||[]);setBusy(false)},250);return()=>clearTimeout(t)},[q]);return <main className="tool-page"><a className="back" href="/">← Workspace</a><section className="tool-hero"><span>ESTARA universal search</span><h1>Find anything, instantly.</h1><p>Properties, people, enquiries, actions and private documents—securely limited to your agency.</p><input autoFocus aria-label="Search" value={q} onChange={e=>setQ(e.target.value)} placeholder="Try a name, phone, reference or suburb…"/></section><section className="results">{busy?<p>Searching…</p>:q.length<2?<p>Type at least two characters to begin.</p>:items.length?items.map(x=><a href={x.href} key={`${x.type}-${x.id}`}><i>{x.type.slice(0,1)}</i><span><small>{x.type}</small><strong>{x.title}</strong><em>{x.detail}</em></span><b>→</b></a>):<p>No matching records in this agency.</p>}</section></main>}
