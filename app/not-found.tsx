import{getPlatformIdentity}from"../db/platform-settings";export default async function NotFound(){const platform=await getPlatformIdentity();return <main className="global-state"><span>{platform.shortName}</span><h1>That page is not available.</h1><p>The address may have expired, been revoked, or never existed. No private information was revealed.</p><div><a href="/workspace">Return to workspace</a><a href="/">{platform.shortName} home</a></div></main>}
