import{requireChatGPTUser}from"../chatgpt-auth";import{getPlatformIdentity}from"../../db/platform-settings";import MatchingClient from"./matching-client";export const dynamic="force-dynamic";export default async function MatchingPage(){await requireChatGPTUser("/matching");const platform=await getPlatformIdentity();return <MatchingClient platform={{shortName:platform.shortName}}/>}
