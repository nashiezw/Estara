import{requireChatGPTUser}from"../chatgpt-auth";import{getPlatformIdentity}from"../../db/platform-settings";import ContactsClient from"./contacts-client";import"./contacts.css";export const dynamic="force-dynamic";export default async function Page(){await requireChatGPTUser("/contacts");const platform=await getPlatformIdentity();return <ContactsClient platform={{shortName:platform.shortName}}/>}
