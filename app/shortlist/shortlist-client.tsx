"use client";import{useEffect,useState}from"react";export default function ShortlistClient({token}:{token:string}){const[data,setData]=useState<any>(null),[error,setError]=useState("");useEffect(()=>{fetch(`/api/public-shortlist?token=${encodeURIComponent(token)}`).then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.error);setData(d)}).catch(e=>setError(e.message))},[token]);if(error)return <main className="shortlist-empty"><span>ESTARA SHORTLIST</span><h1>This shortlist is unavailable.</h1><p>{error}</p></main>;if(!data)return <main className="shortlist-empty"><h1>Preparing your property shortlist…</h1></main>;return <main className="shortlist-page" style={{"--short-primary":data.primaryColor||"#153b34","--short-accent":data.accentColor||"#e6bd5f"}as any}><header><div><strong>{data.agency}</strong><small>PERSONALISED PROPERTY SHORTLIST</small></div><p>Prepared for {data.contact} · private link expires {new Date(data.expiresAt).toLocaleDateString()}</p></header><section><span>CURATED FOR YOU</span><h1>{data.title}</h1><p>Every recommendation below is linked to your saved requirements. The match reasons explain why each property was included.</p><div>{data.items.map((x:any)=><article key={x.id}><div className="shortlist-photo" style={x.imageUrl?{backgroundImage:`url(${x.imageUrl})`}:undefined}><b>{x.score}% match</b></div><section><small>{x.status} · {x.location}</small><h2>{x.title}</h2><strong>{x.price}</strong><p>{x.bedrooms} bedrooms · {x.bathrooms} bathrooms</p><ul>{x.explanation.map((reason:string)=><li key={reason}>{reason}</li>)}</ul></section></article>)}</div></section><footer><div><strong>{data.agency}</strong><p>Your agent can arrange viewings and answer questions about any property in this shortlist.</p></div><div>{data.phone&&<a href={`tel:${data.phone}`}>Call agency</a>}{data.whatsapp&&<a href={`https://wa.me/${String(data.whatsapp).replace(/\D/g,"")}`}>WhatsApp</a>}</div></footer></main>}
