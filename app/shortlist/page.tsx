import{getPlatformIdentity}from"../../db/platform-settings";import ShortlistClient from"./shortlist-client";export const dynamic="force-dynamic";export default async function Shortlist({searchParams}:{searchParams:Promise<{token?:string}>}){const[{token=""},platform]=await Promise.all([searchParams,getPlatformIdentity()]);return <ShortlistClient token={token} platform={{shortName:platform.shortName}}/>}
