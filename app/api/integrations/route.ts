import{env}from"cloudflare:workers";import{getChatGPTUser}from"../../chatgpt-auth";import{requireWorkspace}from"../../../db/workspace";import{AuthorizationError,requirePermission,writeAudit}from"../../../db/authorization";import{PlanLimitError,requireEntitlement,resolveAgencyPlan}from"../../../db/entitlements";const providers={property_portal:{estara_json_feed:"Secure JSON listing feed"},accounting:{estara_csv_ledger:"Exact CSV accounting ledger"}} as const,clean=(v:unknown,n=100)=>typeof v==="string"?v.trim().slice(0,n):"";async function context(){const user=await getChatGPTUser();if(!user)return null;const workspace=await requireWorkspace(user);await requirePermission(workspace,"integration.manage");const plan=await resolveAgencyPlan(workspace.agencyId,user.userId);return{user,workspace,plan}}const fail=(e:unknown)=>Response.json({error:e instanceof Error?e.message:"Integration operation failed."},{status:e instanceof AuthorizationError?403:e instanceof PlanLimitError?402:400});
export async function GET(){try{const c=await context();if(!c)return Response.json({error:"Sign in is required."},{status:401});const[connections,runs]=await Promise.all([env.DB.prepare("SELECT id,kind,provider,status,configuration,approved_by approvedBy,approved_at approvedAt,last_sync_at lastSyncAt,created_at createdAt FROM integration_connections WHERE agency_id=? ORDER BY created_at DESC").bind(c.workspace.agencyId).all<any>(),env.DB.prepare("SELECT id,connection_id connectionId,direction,resource_type resourceType,status,records_read recordsRead,records_written recordsWritten,failure_reason failureReason,idempotency_key idempotencyKey,started_at startedAt,completed_at completedAt FROM integration_sync_runs WHERE agency_id=? ORDER BY started_at DESC LIMIT 100").bind(c.workspace.agencyId).all<any>()]);return Response.json({providers,eligibility:{property_portal:c.plan.entitlements.propertyPortalIntegrations===true,accounting:c.plan.entitlements.accountingIntegrations===true},planName:c.plan.planName,connections:connections.results.map(x=>({...x,configuration:JSON.parse(x.configuration||"{}")})),runs:runs.results})}catch(e){return fail(e)}}
export async function POST(request:Request){try{const c=await context();if(!c)return Response.json({error:"Sign in is required."},{status:401});const b=await request.json(),kind=clean(b.kind,30) as keyof typeof providers,provider=clean(b.provider,50);if(!providers[kind]||!(provider in providers[kind]))throw new Error("Choose a supported integration bridge.");await requireEntitlement(c.workspace.agencyId,c.user.userId,kind==="property_portal"?"propertyPortalIntegrations":"accountingIntegrations");const id=crypto.randomUUID();await env.DB.prepare("INSERT INTO integration_connections(id,agency_id,kind,provider,status,configuration,created_by) VALUES(?,?,?,?, 'pending','{}',?)").bind(id,c.workspace.agencyId,kind,provider,c.user.userId).run();await writeAudit(c.workspace,"integration.connection_created","integration_connection",id,{kind,provider});return Response.json({id,status:"pending"},{status:201})}catch(e){return fail(e)}}
export async function PATCH(request:Request){try{const c=await context();if(!c)return Response.json({error:"Sign in is required."},{status:401});const b=await request.json(),id=clean(b.id),action=clean(b.action,20),row=await env.DB.prepare("SELECT id,kind FROM integration_connections WHERE id=? AND agency_id=?").bind(id,c.workspace.agencyId).first<any>();if(!row)throw new Error("Integration connection was not found.");if(action==="approve"){await requireEntitlement(c.workspace.agencyId,c.user.userId,row.kind==="property_portal"?"propertyPortalIntegrations":"accountingIntegrations");await env.DB.prepare("UPDATE integration_connections SET status='active',approved_by=?,approved_at=CURRENT_TIMESTAMP,updated_at=CURRENT_TIMESTAMP WHERE id=? AND agency_id=? AND status='pending'").bind(c.user.userId,id,c.workspace.agencyId).run();await writeAudit(c.workspace,"integration.connection_approved","integration_connection",id,{kind:row.kind});return Response.json({status:"active"})}if(action==="disable"){await env.DB.prepare("UPDATE integration_connections SET status='disabled',updated_at=CURRENT_TIMESTAMP WHERE id=? AND agency_id=?").bind(id,c.workspace.agencyId).run();await writeAudit(c.workspace,"integration.connection_disabled","integration_connection",id,{kind:row.kind});return Response.json({status:"disabled"})}throw new Error("Unknown integration action.")}catch(e){return fail(e)}}
