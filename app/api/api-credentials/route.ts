import{env}from"cloudflare:workers";import{getChatGPTUser}from"../../chatgpt-auth";import{requireWorkspace}from"../../../db/workspace";import{AuthorizationError,requirePermission,writeAudit}from"../../../db/authorization";import{API_SCOPES,apiKeyHash}from"../../../db/api-auth";const clean=(v:unknown,n=100)=>typeof v==="string"?v.trim().slice(0,n):"";async function ctx(){const user=await getChatGPTUser();if(!user)throw new AuthorizationError("Sign in is required.");const workspace=await requireWorkspace(user);await requirePermission(workspace,"api.manage");return{user,workspace}}const fail=(e:unknown)=>Response.json({error:e instanceof Error?e.message:"Credential operation failed."},{status:e instanceof AuthorizationError?403:400});export async function GET(){try{const{workspace}=await ctx(),rows=await env.DB.prepare("SELECT id,name,key_prefix keyPrefix,scopes,expires_at expiresAt,last_used_at lastUsedAt,revoked_at revokedAt,created_at createdAt FROM api_credentials WHERE agency_id=? ORDER BY created_at DESC").bind(workspace.agencyId).all<any>();return Response.json({allowedScopes:API_SCOPES,credentials:rows.results.map(x=>({...x,scopes:JSON.parse(x.scopes)}))})}catch(e){return fail(e)}}export async function POST(request:Request){try{const{user,workspace}=await ctx(),b=await request.json(),name=clean(b.name),requested=Array.isArray(b.scopes)?b.scopes.map(String):[],scopes=[...new Set(requested)].filter(x=>(API_SCOPES as readonly string[]).includes(x));if(!name||!scopes.length)throw new Error("Name and at least one allowed scope are required.");const token=`est_live_${crypto.randomUUID().replaceAll("-","")}${crypto.randomUUID().replaceAll("-","")}`,id=crypto.randomUUID(),expiresAt=b.expiresAt?new Date(clean(b.expiresAt,40)):null;if(expiresAt&&(!Number.isFinite(expiresAt.getTime())||expiresAt<=new Date()||expiresAt.getTime()>Date.now()+366*864e5))throw new Error("Expiry must be within the next year.");await env.DB.prepare("INSERT INTO api_credentials(id,agency_id,name,key_prefix,key_hash,scopes,expires_at,created_by) VALUES(?,?,?,?,?,?,?,?)").bind(id,workspace.agencyId,name,token.slice(0,17),await apiKeyHash(token),JSON.stringify(scopes),expiresAt?.toISOString()||null,user.userId).run();await writeAudit(workspace,"api_credential.created","api_credential",id,{name,scopes,expiresAt:expiresAt?.toISOString()||null});return Response.json({credential:{id,name,keyPrefix:token.slice(0,17),scopes,expiresAt:expiresAt?.toISOString()||null},token,warning:"Copy this token now. It will not be shown again."},{status:201})}catch(e){return fail(e)}}export async function DELETE(request:Request){try{const{workspace}=await ctx(),id=new URL(request.url).searchParams.get("id")||"",result=await env.DB.prepare("UPDATE api_credentials SET revoked_at=CURRENT_TIMESTAMP WHERE id=? AND agency_id=? AND revoked_at IS NULL").bind(id,workspace.agencyId).run();if(!result.meta.changes)throw new Error("Active credential was not found.");await writeAudit(workspace,"api_credential.revoked","api_credential",id);return Response.json({revoked:true})}catch(e){return fail(e)}}
