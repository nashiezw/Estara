import { env } from "cloudflare:workers";
import { getChatGPTUser } from "../../chatgpt-auth";
import { ensurePlanCatalog } from "../../../db/entitlements";
import { PlatformAuthorizationError,requirePlatformUser,writePlatformAudit,type PlatformContext,type PlatformRole } from "../../../db/platform-auth";
import { calculateInvoiceDiscount,canTransitionSubscription,invoiceTotal } from "../../../db/billing-policy";
export const dynamic="force-dynamic";
const roles=["super_admin","support","finance"] as const,clean=(value:unknown,max=160)=>String(value??"").trim().slice(0,max),emailOk=(value:string)=>/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(value),jsonRecord=(value:unknown)=>value&&typeof value==="object"&&!Array.isArray(value)?value:{};
async function actor(allowed:readonly PlatformRole[]=roles){const user=await getChatGPTUser();if(!user)return null;return{user,context:await requirePlatformUser(user,allowed)}}
const failure=(error:unknown)=>error instanceof PlatformAuthorizationError?Response.json({error:error.message},{status:403}):Response.json({error:"Platform operation could not be completed."},{status:500});
async function event(context:PlatformContext,agencyId:string,subscriptionId:string|null,invoiceId:string|null,eventType:string,detail:Record<string,unknown>={}){await env.DB.prepare("INSERT INTO billing_events (id,agency_id,subscription_id,invoice_id,event_type,detail,actor_user_id) VALUES (?,?,?,?,?,?,?)").bind(crypto.randomUUID(),agencyId,subscriptionId,invoiceId,eventType,JSON.stringify(detail),context.userId).run()}
export async function GET(){try{const access=await actor();if(!access)return Response.json({error:"Sign in is required."},{status:401});await ensurePlanCatalog(access.user.userId);const [platformUsers,plans,agencies,invoices,coupons,events]=await Promise.all([env.DB.prepare("SELECT user_id AS userId,email,role,active,created_at AS createdAt FROM platform_users ORDER BY created_at").all(),env.DB.prepare("SELECT id,plan_key AS planKey,version,name,currency,price_minor AS priceMinor,billing_period AS billingPeriod,entitlements,limits,status,published_at AS publishedAt FROM plan_versions ORDER BY plan_key,version DESC").all(),env.DB.prepare(`SELECT a.id,a.name,a.slug,a.created_at AS createdAt,s.id AS subscriptionId,s.state,s.trial_ends_at AS trialEndsAt,s.grace_ends_at AS graceEndsAt,s.current_period_ends_at AS currentPeriodEndsAt,p.id AS planVersionId,p.name AS planName,(SELECT COUNT(*) FROM agency_memberships m WHERE m.agency_id=a.id) AS users,(SELECT COUNT(*) FROM properties x WHERE x.agency_id=a.id) AS properties,(SELECT COUNT(*) FROM enquiries q WHERE q.agency_id=a.id) AS enquiries FROM agencies a LEFT JOIN agency_subscriptions s ON s.agency_id=a.id LEFT JOIN plan_versions p ON p.id=s.plan_version_id ORDER BY a.created_at DESC`).all(),env.DB.prepare("SELECT i.id,i.agency_id AS agencyId,a.name AS agency,i.subscription_id AS subscriptionId,i.invoice_number AS invoiceNumber,i.status,i.currency,i.subtotal_minor AS subtotalMinor,i.discount_minor AS discountMinor,i.total_minor AS totalMinor,i.due_at AS dueAt,i.issued_at AS issuedAt,i.paid_at AS paidAt,i.payment_method AS paymentMethod,i.provider_reference AS providerReference FROM billing_invoices i JOIN agencies a ON a.id=i.agency_id ORDER BY i.issued_at DESC LIMIT 100").all(),env.DB.prepare("SELECT code,kind,amount,active,valid_until AS validUntil,max_redemptions AS maxRedemptions,redemptions FROM billing_coupons ORDER BY created_at DESC").all(),env.DB.prepare("SELECT b.id,b.agency_id AS agencyId,a.name AS agency,b.event_type AS eventType,b.detail,b.created_at AS createdAt FROM billing_events b JOIN agencies a ON a.id=b.agency_id ORDER BY b.created_at DESC LIMIT 100").all()]);return Response.json({actor:access.context,platformUsers:platformUsers.results,plans:plans.results.map((plan:any)=>({...plan,entitlements:JSON.parse(plan.entitlements||"{}"),limits:JSON.parse(plan.limits||"{}")})),agencies:agencies.results,invoices:invoices.results,coupons:coupons.results,events:events.results})}catch(error){return failure(error)}}
export async function POST(request:Request){try{const body=await request.json() as Record<string,unknown>,action=clean(body.action,40);if(action==="create_platform_user"){const access=await actor(["super_admin"]);if(!access)return Response.json({error:"Sign in is required."},{status:401});const email=clean(body.email).toLowerCase(),userId=clean(body.userId),role=clean(body.role) as PlatformRole;if(!userId||!emailOk(email)||!roles.includes(role))return Response.json({error:"A valid user ID, email and platform role are required."},{status:400});await env.DB.prepare("INSERT INTO platform_users (user_id,email,role,created_by) VALUES (?,?,?,?) ON CONFLICT(user_id) DO UPDATE SET email=excluded.email,role=excluded.role,active=1,updated_at=CURRENT_TIMESTAMP").bind(userId,email,role,access.context.userId).run();await writePlatformAudit(access.context,"platform.user.assigned","platform_user",userId,{email,role});return Response.json({created:true},{status:201})}
if(action==="create_plan_version"){const access=await actor(["super_admin","finance"]);if(!access)return Response.json({error:"Sign in is required."},{status:401});const planKey=clean(body.planKey,50).toLowerCase().replace(/[^a-z0-9-]/g,"-"),name=clean(body.name,80),currency=clean(body.currency,3).toUpperCase(),priceMinor=Math.max(0,Math.round(Number(body.priceMinor))),billingPeriod=clean(body.billingPeriod,20),status=body.status==="published"?"published":"draft",entitlements=jsonRecord(body.entitlements),limits=jsonRecord(body.limits);if(!planKey||!name||!/^[A-Z]{3}$/.test(currency)||!Number.isSafeInteger(priceMinor)||!["month","year"].includes(billingPeriod))return Response.json({error:"Complete every plan field with valid values."},{status:400});const latest=await env.DB.prepare("SELECT COALESCE(MAX(version),0) AS version FROM plan_versions WHERE plan_key=?").bind(planKey).first<{version:number}>(),version=Number(latest?.version||0)+1,id=crypto.randomUUID();await env.DB.prepare("INSERT INTO plan_versions (id,plan_key,version,name,currency,price_minor,billing_period,entitlements,limits,status,published_at,created_by) VALUES (?,?,?,?,?,?,?,?,?,?,?,?)").bind(id,planKey,version,name,currency,priceMinor,billingPeriod,JSON.stringify(entitlements),JSON.stringify(limits),status,status==="published"?new Date().toISOString():null,access.context.userId).run();await writePlatformAudit(access.context,"plan.version.created","plan_version",id,{planKey,version,status});return Response.json({plan:{id,planKey,version,name,status}},{status:201})}
if(action==="assign_subscription"){const access=await actor(["super_admin","finance"]);if(!access)return Response.json({error:"Sign in is required."},{status:401});const agencyId=clean(body.agencyId),planVersionId=clean(body.planVersionId),state=body.state==="active"?"active":"trialing",plan=await env.DB.prepare("SELECT id FROM plan_versions WHERE id=? AND status='published'").bind(planVersionId).first();if(!plan||!await env.DB.prepare("SELECT id FROM agencies WHERE id=?").bind(agencyId).first())return Response.json({error:"Agency or published plan was not found."},{status:404});const now=new Date(),end=new Date(now.getTime()+(state==="trialing"?14:30)*86400000),existing=await env.DB.prepare("SELECT id FROM agency_subscriptions WHERE agency_id=?").bind(agencyId).first<{id:string}>(),subscriptionId=existing?.id||crypto.randomUUID();await env.DB.prepare(`INSERT INTO agency_subscriptions (id,agency_id,plan_version_id,state,trial_starts_at,trial_ends_at,current_period_starts_at,current_period_ends_at,updated_by) VALUES (?,?,?,?,?,?,?,?,?) ON CONFLICT(agency_id) DO UPDATE SET plan_version_id=excluded.plan_version_id,state=excluded.state,trial_starts_at=excluded.trial_starts_at,trial_ends_at=excluded.trial_ends_at,current_period_starts_at=excluded.current_period_starts_at,current_period_ends_at=excluded.current_period_ends_at,grace_ends_at=NULL,suspended_at=NULL,canceled_at=NULL,updated_by=excluded.updated_by,updated_at=CURRENT_TIMESTAMP`).bind(subscriptionId,agencyId,planVersionId,state,state==="trialing"?now.toISOString():null,state==="trialing"?end.toISOString():null,now.toISOString(),end.toISOString(),access.context.userId).run();await event(access.context,agencyId,subscriptionId,null,"subscription.assigned",{planVersionId,state});await writePlatformAudit(access.context,"subscription.assigned","agency_subscription",subscriptionId,{agencyId,planVersionId,state});return Response.json({subscriptionId,state},{status:201})}
if(action==="create_coupon"){const access=await actor(["super_admin","finance"]);if(!access)return Response.json({error:"Sign in is required."},{status:401});const code=clean(body.code,30).toUpperCase().replace(/[^A-Z0-9-]/g,""),kind=body.kind==="fixed"?"fixed":"percent",amount=Math.max(1,Math.round(Number(body.amount))),validUntil=body.validUntil?new Date(String(body.validUntil)).toISOString():null,maxRedemptions=body.maxRedemptions?Math.max(1,Math.round(Number(body.maxRedemptions))):null;if(!code||!Number.isSafeInteger(amount)||(kind==="percent"&&amount>100))return Response.json({error:"Enter a valid coupon."},{status:400});await env.DB.prepare("INSERT INTO billing_coupons (code,kind,amount,valid_until,max_redemptions,created_by) VALUES (?,?,?,?,?,?)").bind(code,kind,amount,validUntil,maxRedemptions,access.context.userId).run();await writePlatformAudit(access.context,"billing.coupon.created","billing_coupon",code,{kind,amount});return Response.json({code},{status:201})}
if(action==="create_invoice"){const access=await actor(["super_admin","finance"]);if(!access)return Response.json({error:"Sign in is required."},{status:401});const agencyId=clean(body.agencyId),subscription=await env.DB.prepare("SELECT s.id,p.price_minor AS priceMinor,p.currency FROM agency_subscriptions s JOIN plan_versions p ON p.id=s.plan_version_id WHERE s.agency_id=?").bind(agencyId).first<{id:string;priceMinor:number;currency:string}>();if(!subscription)return Response.json({error:"Agency subscription was not found."},{status:404});const couponCode=clean(body.couponCode,30).toUpperCase(),coupon=couponCode?await env.DB.prepare("SELECT kind,amount FROM billing_coupons WHERE code=? AND active=1 AND (valid_until IS NULL OR valid_until>CURRENT_TIMESTAMP) AND (max_redemptions IS NULL OR redemptions<max_redemptions)").bind(couponCode).first<{kind:string;amount:number}>():null;if(couponCode&&!coupon)return Response.json({error:"Coupon is invalid or exhausted."},{status:400});const subtotal=subscription.priceMinor,discount=calculateInvoiceDiscount(subtotal,coupon),total=invoiceTotal(subtotal,coupon),id=crypto.randomUUID(),number=`EST-${Date.now().toString().slice(-10)}`,dueAt=new Date(Date.now()+7*86400000).toISOString();await env.DB.batch([env.DB.prepare("INSERT INTO billing_invoices (id,agency_id,subscription_id,invoice_number,status,currency,subtotal_minor,discount_minor,total_minor,due_at,created_by) VALUES (?,?,?,?,?,?,?,?,?,?,?)").bind(id,agencyId,subscription.id,number,"open",subscription.currency,subtotal,discount,total,dueAt,access.context.userId),...(coupon?[env.DB.prepare("UPDATE billing_coupons SET redemptions=redemptions+1 WHERE code=?").bind(couponCode)]:[])]);await event(access.context,agencyId,subscription.id,id,"invoice.issued",{number,subtotal,discount,total,couponCode:couponCode||null});await writePlatformAudit(access.context,"billing.invoice.issued","billing_invoice",id,{agencyId,number,total});return Response.json({invoice:{id,number,total,currency:subscription.currency,dueAt}},{status:201})}return Response.json({error:"Unsupported platform action."},{status:400})}catch(error){return failure(error)}}
export async function PATCH(request:Request){try{const body=await request.json() as Record<string,unknown>,action=clean(body.action,40);if(action==="transition_subscription"){const access=await actor(["super_admin","finance","support"]);if(!access)return Response.json({error:"Sign in is required."},{status:401});const id=clean(body.id),next=clean(body.state),subscription=await env.DB.prepare("SELECT id,agency_id AS agencyId,state FROM agency_subscriptions WHERE id=?").bind(id).first<{id:string;agencyId:string;state:string}>();if(!subscription)return Response.json({error:"Subscription was not found."},{status:404});if(!canTransitionSubscription(subscription.state,next))return Response.json({error:`Cannot move a subscription from ${subscription.state} to ${next}.`},{status:409});const graceEnds=next==="grace"?new Date(Date.now()+7*86400000).toISOString():null,suspendedAt=next==="suspended"?new Date().toISOString():null,canceledAt=next==="canceled"?new Date().toISOString():null;await env.DB.prepare("UPDATE agency_subscriptions SET state=?,grace_ends_at=?,suspended_at=?,canceled_at=?,updated_by=?,updated_at=CURRENT_TIMESTAMP WHERE id=?").bind(next,graceEnds,suspendedAt,canceledAt,access.context.userId,id).run();await event(access.context,subscription.agencyId,id,null,"subscription.state_changed",{from:subscription.state,to:next});await writePlatformAudit(access.context,"subscription.state.changed","agency_subscription",id,{from:subscription.state,to:next});return Response.json({state:next})}
if(action==="mark_invoice_paid"){const access=await actor(["super_admin","finance"]);if(!access)return Response.json({error:"Sign in is required."},{status:401});const id=clean(body.id),reference=clean(body.reference,120),method=clean(body.method,40);if(!reference||!method)return Response.json({error:"Payment method and receipt reference are required."},{status:400});const invoice=await env.DB.prepare("SELECT id,agency_id AS agencyId,subscription_id AS subscriptionId FROM billing_invoices WHERE id=? AND status='open'").bind(id).first<{id:string;agencyId:string;subscriptionId:string}>();if(!invoice)return Response.json({error:"Open invoice was not found."},{status:404});await env.DB.batch([env.DB.prepare("UPDATE billing_invoices SET status='paid',paid_at=CURRENT_TIMESTAMP,payment_method=?,provider_reference=? WHERE id=? AND status='open'").bind(method,reference,id),env.DB.prepare("UPDATE agency_subscriptions SET state='active',grace_ends_at=NULL,suspended_at=NULL,updated_by=?,updated_at=CURRENT_TIMESTAMP WHERE id=? AND state<>'canceled'").bind(access.context.userId,invoice.subscriptionId)]);await event(access.context,invoice.agencyId,invoice.subscriptionId,id,"invoice.paid",{method,reference});await writePlatformAudit(access.context,"billing.invoice.paid","billing_invoice",id,{method,reference});return Response.json({paid:true})}return Response.json({error:"Unsupported platform action."},{status:400})}catch(error){return failure(error)}}
