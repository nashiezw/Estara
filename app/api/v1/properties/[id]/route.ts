import{env}from"cloudflare:workers";import{logApiRequest,requireApiCredential}from"../../../../../db/api-auth";import{clean,idempotent,propertyPayload,remember,updateProperty}from"../../../../../db/public-api";const route="/api/v1/properties/:id";export async function GET(request:Request,{params}:{params:Promise<{id:string}>}){let credential;try{credential=await requireApiCredential(request,"properties:read");const{id}=await params,row=await env.DB.prepare("SELECT id,reference,title,location,price_minor priceMinor,currency,transaction_type transactionType,property_type propertyType,price_label priceLabel,bedrooms,bathrooms,toilets,parking,garages,status,land_size landSize,building_size buildingSize,country,province,city,suburb,address,latitude,longitude,description,features,photo_count photoCount,created_at createdAt,updated_at updatedAt FROM properties WHERE id=? AND agency_id=?").bind(id,credential.agencyId).first<any>();if(!row)return Response.json({error:"Property was not found."},{status:404});const media=await env.DB.prepare("SELECT id,category,sort_order sortOrder FROM media_assets WHERE agency_id=? AND property_id=? AND kind='property_photo' ORDER BY sort_order,created_at").bind(credential.agencyId,id).all<any>();await logApiRequest(credential,route,"GET",200);return Response.json({data:{...propertyPayload(row),media:media.results.map((x:any)=>({...x,url:`/api/media?id=${encodeURIComponent(x.id)}`,thumbnailUrl:`/api/media?id=${encodeURIComponent(x.id)}&variant=thumb`}))}},{headers:{"cache-control":"private, no-store"}})}catch(e){if(credential)await logApiRequest(credential,route,"GET",400);return Response.json({error:e instanceof Error?e.message:"API request failed."},{status:400})}}export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){let credential;try{credential=await requireApiCredential(request,"properties:write");const{id}=await params,body=await request.json(),key=clean(request.headers.get("idempotency-key"),100);let idem:{keyHash:string;existing:any}|null=null;if(key){idem=await idempotent(credential,`${route}:PATCH:${id}`,key);if(idem.existing)return new Response(idem.existing.body,{status:idem.existing.status,headers:{"content-type":"application/json","x-idempotent-replay":"true"}})}const updated=await updateProperty(credential,id,body),response=JSON.stringify({data:updated});if(idem)await remember(credential,`${route}:PATCH:${id}`,idem.keyHash,200,response);await logApiRequest(credential,route,"PATCH",200);return new Response(response,{headers:{"content-type":"application/json"}})}catch(e){if(credential)await logApiRequest(credential,route,"PATCH",400);return Response.json({error:e instanceof Error?e.message:"API request failed."},{status:400})}}
