import{env}from"cloudflare:workers";import{logApiRequest,requireApiCredential}from"../../../../db/api-auth";import{publishDomainEvent}from"../../../../db/automation";import{reminderTime,validViewingWindow}from"../../../../db/viewing-policy";import{apiAudit,clean,idempotent,remember}from"../../../../db/public-api";const route="/api/v1/bookings";export async function POST(request:Request){let credential;try{credential=await requireApiCredential(request,"bookings:write");const idem=await idempotent(credential,route,request.headers.get("idempotency-key")||"");if(idem.existing)return new Response(idem.existing.body,{status:idem.existing.status,headers:{"content-type":"application/json","x-idempotent-replay":"true"}});const b=await request.json(),propertyId=clean(b.propertyId,100),startsAt=clean(b.startsAt,40),endsAt=clean(b.endsAt,40),fullName=clean(b.fullName||b.name,150),phone=clean(b.phone,30)||null,email=clean(b.email,180).toLowerCase()||null,notes=clean(b.notes,800);if(!propertyId||!validViewingWindow(startsAt,endsAt)||!fullName||!phone&&!email)throw new Error("Property, client, contact detail and a valid booking window are required.");const property=await env.DB.prepare("SELECT id,title,listing_agent_id listingAgentId FROM properties WHERE id=? AND agency_id=? AND status='Available'").bind(propertyId,credential.agencyId).first<any>();if(!property)throw new Error("Published property was not found.");let contact=phone?await env.DB.prepare("SELECT id FROM contacts WHERE agency_id=? AND phone_e164=?").bind(credential.agencyId,phone).first<any>():null;if(!contact&&email)contact=await env.DB.prepare("SELECT id FROM contacts WHERE agency_id=? AND email_normalized=?").bind(credential.agencyId,email).first<any>();const contactId=contact?.id||crypto.randomUUID(),start=new Date(Date.parse(startsAt)).toISOString(),end=new Date(Date.parse(endsAt)).toISOString(),assignedUserId=property.listingAgentId||"api",viewingId=crypto.randomUUID(),reminder=reminderTime(start),statements=[];if(!contact)statements.push(env.DB.prepare("INSERT INTO contacts(id,agency_id,full_name,phone_e164,email_normalized,roles,created_by) VALUES(?,?,?,?,?,'[\"buyer\"]',?)").bind(contactId,credential.agencyId,fullName,phone,email,`api:${credential.id}`));statements.push(env.DB.prepare("INSERT INTO viewings(id,agency_id,property_id,contact_id,assigned_user_id,starts_at,ends_at,status,notes,reminder_at,created_by) VALUES(?,?,?,?,?,?,?,'Requested',?,?,?)").bind(viewingId,credential.agencyId,propertyId,contactId,assignedUserId,start,end,notes,reminder,`api:${credential.id}`));await env.DB.batch(statements);await apiAudit(credential,"api.booking.requested","viewing",viewingId,{propertyId,contactId,startsAt:start});await publishDomainEvent(credential.agencyId,"viewing.requested","viewing",viewingId,{assignedUserId,property:property.title,resourceType:"viewing",resourceId:viewingId,startsAt:start,source:"Public API"});const body=JSON.stringify({data:{id:viewingId,propertyId,contactId,assignedUserId,startsAt:start,endsAt:end,status:"Requested",reminderAt:reminder}});await remember(credential,route,idem.keyHash,201,body);await logApiRequest(credential,route,"POST",201);return new Response(body,{status:201,headers:{"content-type":"application/json"}})}catch(e){if(credential)await logApiRequest(credential,route,"POST",400);return Response.json({error:e instanceof Error?e.message:"API request failed."},{status:400})}}
