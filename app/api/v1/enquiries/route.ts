import{env}from"cloudflare:workers";import{logApiRequest,requireApiCredential}from"../../../../db/api-auth";import{publishDomainEvent}from"../../../../db/automation";import{apiAudit,clean,idempotent,remember}from"../../../../db/public-api";const route="/api/v1/enquiries";export async function GET(request:Request){let credential;try{credential=await requireApiCredential(request,"enquiries:read");const url=new URL(request.url),cursor=url.searchParams.get("cursor")||"",limit=Math.min(100,Math.max(1,Number(url.searchParams.get("limit")||25))),rows=await env.DB.prepare("SELECT id,property_id propertyId,contact_id contactId,contact_name fullName,property_label property,status,stage,source,response_due_at responseDueAt,next_follow_up_at nextFollowUpAt,created_at createdAt FROM enquiries WHERE agency_id=? AND id>? ORDER BY id LIMIT ?").bind(credential.agencyId,cursor,limit+1).all<any>(),hasMore=rows.results.length>limit,items=rows.results.slice(0,limit);await logApiRequest(credential,route,"GET",200);return Response.json({data:items,nextCursor:hasMore?items.at(-1)?.id:null},{headers:{"cache-control":"private, no-store"}})}catch(e){if(credential)await logApiRequest(credential,route,"GET",400);return Response.json({error:e instanceof Error?e.message:"API request failed."},{status:400})}}export async function POST(request:Request){let credential;try{credential=await requireApiCredential(request,"enquiries:write");const idem=await idempotent(credential,route,request.headers.get("idempotency-key")||"");if(idem.existing)return new Response(idem.existing.body,{status:idem.existing.status,headers:{"content-type":"application/json","x-idempotent-replay":"true"}});const b=await request.json(),propertyId=clean(b.propertyId,100),fullName=clean(b.fullName||b.name,150),phone=clean(b.phone,30)||null,email=clean(b.email,180).toLowerCase()||null,requirements=clean(b.requirements,1000),property=propertyId?await env.DB.prepare("SELECT id,title,listing_agent_id listingAgentId FROM properties WHERE id=? AND agency_id=? AND status='Available'").bind(propertyId,credential.agencyId).first<any>():null;if(propertyId&&!property)throw new Error("Published property was not found.");if(!fullName||!phone&&!email)throw new Error("Name and phone or email are required.");let contact=phone?await env.DB.prepare("SELECT id FROM contacts WHERE agency_id=? AND phone_e164=?").bind(credential.agencyId,phone).first<any>():null;if(!contact&&email)contact=await env.DB.prepare("SELECT id FROM contacts WHERE agency_id=? AND email_normalized=?").bind(credential.agencyId,email).first<any>();const contactId=contact?.id||crypto.randomUUID(),enquiryId=crypto.randomUUID(),settings=await env.DB.prepare("SELECT response_sla_minutes sla FROM agency_settings WHERE agency_id=?").bind(credential.agencyId).first<any>(),due=new Date(Date.now()+Number(settings?.sla||30)*60000).toISOString(),initials=fullName.split(/\s+/).map(x=>x[0]).join("").slice(0,2).toUpperCase(),statements=[];if(!contact)statements.push(env.DB.prepare("INSERT INTO contacts(id,agency_id,full_name,phone_e164,email_normalized,roles,requirements,created_by) VALUES(?,?,?,?,?,'[\"buyer\"]',?,?)").bind(contactId,credential.agencyId,fullName,phone,email,requirements,`api:${credential.id}`));statements.push(env.DB.prepare("INSERT INTO enquiries(id,agency_id,property_id,contact_id,assigned_user_id,contact_name,initials,property_label,status,stage,source,response_due_at) VALUES(?,?,?,?,?,?,?,?,'New','New','Public API',?)").bind(enquiryId,credential.agencyId,propertyId||null,contactId,property?.listingAgentId||null,fullName,initials,property?.title||"General enquiry",due));await env.DB.batch(statements);await apiAudit(credential,"api.enquiry.created","enquiry",enquiryId,{contactId,propertyId:propertyId||null});await publishDomainEvent(credential.agencyId,"enquiry.created","enquiry",enquiryId,{name:fullName,property:property?.title||"General enquiry",assignedUserId:property?.listingAgentId||"",resourceType:"enquiry",resourceId:enquiryId,source:"Public API",responseDueAt:due});const body=JSON.stringify({data:{id:enquiryId,contactId,responseDueAt:due}});await remember(credential,route,idem.keyHash,201,body);await logApiRequest(credential,route,"POST",201);return new Response(body,{status:201,headers:{"content-type":"application/json"}})}catch(e){if(credential)await logApiRequest(credential,route,"POST",400);return Response.json({error:e instanceof Error?e.message:"API request failed."},{status:400})}}
