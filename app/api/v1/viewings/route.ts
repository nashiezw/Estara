import{env}from"cloudflare:workers";import{logApiRequest,requireApiCredential}from"../../../../db/api-auth";import{publishDomainEvent}from"../../../../db/automation";import{reminderTime,validViewingWindow}from"../../../../db/viewing-policy";import{apiAudit,clean,idempotent,remember}from"../../../../db/public-api";const route="/api/v1/viewings";export async function GET(request:Request){let credential;try{credential=await requireApiCredential(request,"viewings:read");const url=new URL(request.url),cursor=url.searchParams.get("cursor")||"",limit=Math.min(100,Math.max(1,Number(url.searchParams.get("limit")||25))),rows=await env.DB.prepare("SELECT id,property_id propertyId,enquiry_id enquiryId,contact_id contactId,assigned_user_id assignedUserId,starts_at startsAt,ends_at endsAt,status,notes,feedback,interest_level interestLevel,created_at createdAt,updated_at updatedAt FROM viewings WHERE agency_id=? AND id>? ORDER BY id LIMIT ?").bind(credential.agencyId,cursor,limit+1).all<any>(),hasMore=rows.results.length>limit,items=rows.results.slice(0,limit);await logApiRequest(credential,route,"GET",200);return Response.json({data:items,nextCursor:hasMore?items.at(-1)?.id:null},{headers:{"cache-control":"private, no-store"}})}catch(e){if(credential)await logApiRequest(credential,route,"GET",400);return Response.json({error:e instanceof Error?e.message:"API request failed."},{status:400})}}export async function POST(request:Request){let credential;try{credential=await requireApiCredential(request,"viewings:write");const idem=await idempotent(credential,route,request.headers.get("idempotency-key")||"");if(idem.existing)return new Response(idem.existing.body,{status:idem.existing.status,headers:{"content-type":"application/json","x-idempotent-replay":"true"}});const b=await request.json(),propertyId=clean(b.propertyId,100),enquiryId=clean(b.enquiryId,100)||null,contactId=clean(b.contactId,100)||null,startsAt=clean(b.startsAt,40),endsAt=clean(b.endsAt,40),notes=clean(b.notes,800);if(!propertyId||!validViewingWindow(startsAt,endsAt))throw new Error("Choose a valid property and viewing time.");const property=await env.DB.prepare("SELECT id,title,listing_agent_id listingAgentId FROM properties WHERE id=? AND agency_id=? AND status='Available'").bind(propertyId,credential.agencyId).first<any>();if(!property)throw new Error("Published property was not found.");const start=new Date(Date.parse(startsAt)).toISOString(),end=new Date(Date.parse(endsAt)).toISOString(),assignedUserId=clean(b.assignedUserId,100)||property.listingAgentId||"api",conflict=await env.DB.prepare("SELECT id FROM viewings WHERE agency_id=? AND status IN ('Requested','Confirmed') AND starts_at<? AND ends_at>? AND (property_id=? OR assigned_user_id=?) LIMIT 1").bind(credential.agencyId,end,start,propertyId,assignedUserId).first();if(conflict)throw new Error("The property or assigned agent already has a viewing at that time.");const id=crypto.randomUUID(),reminder=reminderTime(start);await env.DB.prepare("INSERT INTO viewings(id,agency_id,property_id,enquiry_id,contact_id,assigned_user_id,starts_at,ends_at,status,notes,reminder_at,created_by) VALUES(?,?,?,?,?,?,?,?,'Requested',?,?,?)").bind(id,credential.agencyId,propertyId,enquiryId,contactId,assignedUserId,start,end,notes,reminder,`api:${credential.id}`).run();await apiAudit(credential,"api.viewing.requested","viewing",id,{propertyId,startsAt:start});await publishDomainEvent(credential.agencyId,"viewing.requested","viewing",id,{assignedUserId,property:property.title,resourceType:"viewing",resourceId:id,startsAt:start,source:"Public API"});const body=JSON.stringify({data:{id,propertyId,enquiryId,contactId,assignedUserId,startsAt:start,endsAt:end,status:"Requested",reminderAt:reminder}});await remember(credential,route,idem.keyHash,201,body);await logApiRequest(credential,route,"POST",201);return new Response(body,{status:201,headers:{"content-type":"application/json"}})}catch(e){if(credential)await logApiRequest(credential,route,"POST",400);return Response.json({error:e instanceof Error?e.message:"API request failed."},{status:400})}}
