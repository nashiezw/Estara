import { env } from "cloudflare:workers";import { getChatGPTUser } from "../../chatgpt-auth";import { requireWorkspace } from "../../../db/workspace";import { AuthorizationError,requirePermission,writeAudit } from "../../../db/authorization";export const dynamic="force-dynamic";const safe=(v:unknown,n=120)=>typeof v==="string"?v.trim().slice(0,n):"";
export async function GET(){const u=await getChatGPTUser();if(!u)return Response.json({error:"Sign in is required."},{status:401});const w=await requireWorkspace(u);const [platform,agency]=await Promise.all([env.DB.prepare("SELECT * FROM platform_settings WHERE id='default'").first(),env.DB.prepare("SELECT a.name,s.* FROM agencies a JOIN agency_settings s ON s.agency_id=a.id WHERE a.id=?").bind(w.agencyId).first()]);return Response.json({platform,agency})}
export async function PATCH(r:Request){try{const u=await getChatGPTUser();if(!u)return Response.json({error:"Sign in is required."},{status:401});const w=await requireWorkspace(u);await requirePermission(w,"agency.settings.manage");const b=await r.json() as Record<string,unknown>,name=safe(b.name),tagline=safe(b.tagline),primary=safe(b.primaryColor,20),accent=safe(b.accentColor,20),phone=safe(b.phone,40),whatsapp=safe(b.whatsapp,40),email=safe(b.email,160),website=safe(b.website,240);if(!name||!/^#[0-9a-f]{6}$/i.test(primary)||!/^#[0-9a-f]{6}$/i.test(accent))return Response.json({error:"A name and valid brand colours are required."},{status:400});await env.DB.batch([env.DB.prepare("UPDATE agencies SET name=? WHERE id=?").bind(name,w.agencyId),env.DB.prepare("UPDATE agency_settings SET tagline=?,primary_color=?,accent_color=?,phone=?,whatsapp=?,email=?,website=?,updated_at=CURRENT_TIMESTAMP WHERE agency_id=?").bind(tagline,primary,accent,phone,whatsapp,email,website,w.agencyId)]);await writeAudit(w,"agency.settings.updated","agency",w.agencyId,{name,primary,accent});return Response.json({agency:{name,tagline,primaryColor:primary,accentColor:accent,phone,whatsapp,email,website}})}catch(e){if(e instanceof AuthorizationError)return Response.json({error:e.message},{status:403});return Response.json({error:"Settings could not be updated."},{status:500})}}
