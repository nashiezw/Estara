import{requireChatGPTUser}from"../chatgpt-auth";import{getPlatformIdentity}from"../../db/platform-settings";import PlatformToolHeader from"../components/PlatformToolHeader";import AuditClient from"./audit-client";import"../workspace-tools.css";export const dynamic="force-dynamic";export default async function Page(){await requireChatGPTUser("/audit");const platform=await getPlatformIdentity(),brand={shortName:platform.shortName,platformName:platform.platformName,logoUrl:platform.logoUrl,iconUrl:platform.iconUrl,primaryColor:platform.primaryColor,accentColor:platform.accentColor};return <main className="tool-page"><PlatformToolHeader platform={brand} section="Governance"/><AuditClient/></main>}
