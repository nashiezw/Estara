import type{Metadata}from"next";import{notFound}from"next/navigation";import{getPublicAgency,listPublicProperties}from"../../../../db/public-site";import{PublicSection}from"../public-website";export const dynamic="force-dynamic";export const revalidate=300;const ALLOWED=new Set(["properties","sale","rent","agents","services","about","contact"]);
export async function generateMetadata({params}:{params:Promise<{slug:string;section:string}>}):Promise<Metadata>{const{slug,section}=await params;if(!ALLOWED.has(section))return{robots:{index:false,follow:false}};const a=await getPublicAgency(slug);if(!a)return{robots:{index:false,follow:false}};return{title:`${section[0].toUpperCase()+section.slice(1)} | ${a.name}`,description:`${section} from ${a.name}.`,alternates:{canonical:`/site/${slug}/${section}`}}}
export default async function Page({params}:{params:Promise<{slug:string;section:string}>}){const{slug,section}=await params;if(!ALLOWED.has(section))notFound();const a=await getPublicAgency(slug);if(!a)notFound();const type=section==="sale"?"Sale":section==="rent"?"Rent":undefined,properties=await listPublicProperties(a.id,type);return <PublicSection agency={a} properties={properties} section={section}/>}