import type{Metadata}from"next";import{notFound}from"next/navigation";import{getPublicAgency,listPublicProperties}from"../../../db/public-site";import{PageView}from"./public-client";import{PublicHome}from"./public-website";export const dynamic="force-dynamic";export const revalidate=300;
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params,a=await getPublicAgency(slug);if(!a)return{title:"Agency not found",robots:{index:false,follow:false}};return{title:`${a.name} | Property in Zimbabwe`,description:a.tagline||`Live property listings from ${a.name}.`,alternates:{canonical:`/site/${a.slug}`},robots:{index:true,follow:true},openGraph:{title:a.name,description:a.tagline,type:"website"}}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params,a=await getPublicAgency(slug);if(!a)notFound();const properties=await listPublicProperties(a.id);return <><PageView slug={slug}/><PublicHome agency={a} properties={properties}/></>}