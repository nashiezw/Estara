import{env}from"cloudflare:workers";
export type PublicAgency={id:string;name:string;slug:string;tagline:string;primaryColor:string;accentColor:string;phone:string;whatsapp:string;email:string;website:string;businessActivities:string[];websiteTemplate:string;responseSlaMinutes:number};
export type PublicProperty={id:string;ref:string;title:string;location:string;price:string;beds:number;baths:number;photos:number;size:string;transactionType:string};
export async function getPublicAgency(slug:string):Promise<PublicAgency|null>{if(!/^[a-z][a-z0-9-]{3,58}[a-z0-9]$/.test(slug)||slug.includes("--"))return null;const row=await env.DB.prepare(`SELECT a.id,a.name,a.slug,s.tagline,s.primary_color AS primaryColor,s.accent_color AS accentColor,s.phone,s.whatsapp,s.email,s.website,s.business_activities AS businessActivities,s.website_template AS websiteTemplate,s.response_sla_minutes AS responseSlaMinutes FROM agencies a JOIN agency_settings s ON s.agency_id=a.id WHERE a.slug=? AND s.onboarding_complete=1`).bind(slug).first<any>();if(!row)return null;return{...row,businessActivities:JSON.parse(row.businessActivities||"[]")}}
export async function listPublicProperties(agencyId:string,transactionType?:string){const filter=transactionType?" AND transaction_type=?":"",query=`SELECT id,reference AS ref,title,location,price_label AS price,bedrooms AS beds,bathrooms AS baths,photo_count AS photos,land_size AS size,transaction_type AS transactionType FROM properties WHERE agency_id=? AND status='Available'${filter} ORDER BY updated_at DESC`;const result=transactionType?await env.DB.prepare(query).bind(agencyId,transactionType).all<PublicProperty>():await env.DB.prepare(query).bind(agencyId).all<PublicProperty>();return result.results}
export async function getPublicProperty(agencyId:string,id:string){return env.DB.prepare(`SELECT id,reference AS ref,title,location,price_label AS price,bedrooms AS beds,bathrooms AS baths,photo_count AS photos,land_size AS size,transaction_type AS transactionType FROM properties WHERE id=? AND agency_id=? AND status='Available'`).bind(id,agencyId).first<PublicProperty>()}