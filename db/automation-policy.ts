export type Condition={field:string;operator:"equals"|"not_equals"|"includes"|"exists";value?:unknown};
export type AutomationAction={type:"notification"|"next_action";recipientField:string;title?:string;body?:string;actionType?:string;reason?:string;priority?:"low"|"normal"|"high";dueMinutes?:number;resourceTypeField?:string;resourceIdField?:string};
export function conditionsMatch(payload:Record<string,unknown>,conditions:Condition[]){return conditions.every(condition=>{const value=payload[condition.field];if(condition.operator==="exists")return value!==undefined&&value!==null&&value!=="";if(condition.operator==="equals")return value===condition.value;if(condition.operator==="not_equals")return value!==condition.value;if(condition.operator==="includes")return Array.isArray(value)?value.includes(condition.value):String(value??"").includes(String(condition.value??""));return false})}
export function renderTemplate(template:string,payload:Record<string,unknown>){return template.replace(/\{\{([a-zA-Z0-9_]+)\}\}/g,(_,key)=>Object.prototype.hasOwnProperty.call(payload,key)?String(payload[key]??""):"")}
export function retryDelayMs(attempt:number){return Math.min(6*60*60*1000,Math.max(60_000,2**Math.max(0,attempt-1)*60_000))}
export function validateActions(value:unknown):AutomationAction[]{if(!Array.isArray(value))return[];return value.filter((item):item is AutomationAction=>Boolean(item&&typeof item==="object"&&["notification","next_action"].includes(String((item as any).type))&&String((item as any).recipientField||"").length>0)).slice(0,10)}
