import { buildPdf } from "./marketing-pdf.ts";

const safe=(value:unknown)=>String(value??"").normalize("NFKD").replace(/[^\x20-\x7E]/g,"-").replace(/[\\()]/g,"-");
const text=(value:unknown,x:number,y:number,size=11,font="F1",color="0.06 0.23 0.20")=>`${color} rg BT /${font} ${size} Tf ${x} ${y} Td (${safe(value)}) Tj ET`;
function wrap(value:unknown,x:number,y:number,width:number,size=11,leading=16,font="F1",color="0.06 0.23 0.20"){const words=safe(value).split(/\s+/),limit=Math.max(20,Math.floor(width/(size*.55)));let line="",cursor=y,out="";for(const word of words){if((line+" "+word).trim().length>limit){out+=" "+text(line,x,cursor,size,font,color);line=word;cursor-=leading}else line=(line+" "+word).trim()}return out+" "+text(line,x,cursor,size,font,color)}
export type SellerPdfInput={agency:string;property:string;reference:string;location:string;periodStart:string;periodEnd:string;views:number;enquiries:number;viewings:number;offers:number;momentum:string;summary:string;recommendedAction:string;approvedAt:string};
export function sellerReportPdf(r:SellerPdfInput){let page="0.06 0.23 0.20 rg 0 0 595 842 re f 0.85 0.75 0.35 rg 40 777 84 5 re f ";page+=text(r.agency.toUpperCase(),40,745,12,"F2","1 1 1")+" "+text("SELLER PERFORMANCE REPORT",40,700,11,"F2","0.85 0.75 0.35")+" "+wrap(r.property,40,647,510,30,37,"F3","1 1 1")+" "+text(`${r.reference} | ${r.location}`,40,558,11,"F1","0.82 0.89 0.86")+" "+text(`${new Date(r.periodStart).toLocaleDateString("en-ZW")} - ${new Date(r.periodEnd).toLocaleDateString("en-ZW")}`,40,532,10,"F1","0.82 0.89 0.86");const metrics=[[r.views,"LISTING VIEWS"],[r.enquiries,"ENQUIRIES"],[r.viewings,"VIEWINGS"],[r.offers,"OFFERS"]] as const;metrics.forEach(([value,label],i)=>{const x=40+i*130;page+=" 0.09 0.30 0.26 rg "+`${x} 390 118 105 re f `+text(value,x+16,444,26,"F3","1 1 1")+" "+text(label,x+16,414,8,"F2","0.85 0.75 0.35")});page+=" "+text("PROPERTY MOMENTUM",40,347,9,"F2","0.85 0.75 0.35")+" "+text(r.momentum,40,318,20,"F3","1 1 1")+" "+wrap(r.summary,40,270,510,11,16,"F1","0.86 0.91 0.89")+" 0.85 0.75 0.35 rg 40 107 4 90 re f "+text("RECOMMENDED NEXT STEP",60,170,9,"F2","0.85 0.75 0.35")+" "+wrap(r.recommendedAction,60,143,480,11,16,"F1","1 1 1")+" "+text(`Agency approved ${new Date(r.approvedAt).toLocaleDateString("en-ZW")} | Verified ESTARA activity`,40,45,8,"F1","0.7 0.8 0.76");return buildPdf([page])}
