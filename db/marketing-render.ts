import{env}from"cloudflare:workers";import QRCode from"../vendor/qrcode/lib/browser.js";import{brochurePdf}from"./marketing-pdf";
export const MARKETING_FORMATS={whatsapp_card:{name:"WhatsApp card",width:1200,height:628,kind:"creative.svg"},whatsapp_status:{name:"WhatsApp status",width:1080,height:1920,kind:"creative.svg"},facebook_square:{name:"Facebook 1:1",width:1080,height:1080,kind:"creative.svg"},facebook_portrait:{name:"Facebook 4:5",width:1080,height:1350,kind:"creative.svg"},instagram_post:{name:"Instagram post",width:1080,height:1080,kind:"creative.svg"},instagram_story:{name:"Instagram story",width:1080,height:1920,kind:"creative.svg"},flyer:{name:"Property flyer",width:595,height:842,kind:"flyer.pdf"},brochure:{name:"Property brochure",width:595,height:842,kind:"brochure.pdf"},qr:{name:"Share QR code",width:600,height:600,kind:"qr.svg"}}as const;export type MarketingFormat=keyof typeof MARKETING_FORMATS;
const esc=(value:unknown)=>String(value??"").replace(/[&<>"']/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&apos;"}[char]!));const money=(minor:number,currency:string)=>`${currency} ${(minor/100).toLocaleString("en-US",{maximumFractionDigits:0})}`;const bucket=()=>{const value=(env as unknown as{MEDIA?:R2Bucket}).MEDIA;if(!value)throw new Error("Private media storage is unavailable.");return value};
export function factualCopy(property:any){const facts=[property.bedrooms?`${property.bedrooms} bedrooms`:"",property.bathrooms?`${property.bathrooms} bathrooms`:"",property.landSize?`${property.landSize} land`:"",property.features?.length?property.features.slice(0,5).join(", "):""].filter(Boolean);const place=[property.suburb,property.city].filter(Boolean).join(", ")||property.location;const headline=`${property.propertyType||"Property"} for ${String(property.transactionType||"sale").toLowerCase()} in ${place}`;const listingDescription=[property.description,`${money(property.priceMinor,property.currency)}.`,facts.length?`Key facts: ${facts.join("; ")}.`:""].filter(Boolean).join(" ");const socialCaption=[headline,money(property.priceMinor,property.currency),facts.join(" · "),property.reference?`Ref: ${property.reference}`:""].filter(Boolean).join("\n");return{headline,listingDescription,socialCaption,facts}}
export function creativeSvg(snapshot:any,width:number,height:number){const p=snapshot.property,a=snapshot.agency,copy=snapshot.copy,portrait=height/width>1.3,titleSize=portrait?64:52,price=money(p.priceMinor,p.currency),facts=copy.facts.slice(0,4),typography=String(a.typography||"classic"),headingFont=typography==="modern"?"Arial":typography==="editorial"?"Times New Roman":"Georgia",bodyFont=typography==="editorial"?"Georgia":"Arial";return`<svg xmlns="http://www.w3.org/2000/svg" width="${width}" height="${height}" viewBox="0 0 ${width} ${height}"><rect width="100%" height="100%" fill="${esc(a.primaryColor)}"/><circle cx="${width*.88}" cy="${height*.12}" r="${Math.min(width,height)*.26}" fill="${esc(a.accentColor)}" opacity=".14"/><rect x="${width*.07}" y="${height*.08}" width="${width*.12}" height="6" rx="3" fill="${esc(a.accentColor)}"/><text x="${width*.07}" y="${height*.16}" fill="#fff" font-family="${bodyFont}" font-weight="700" font-size="${Math.round(width*.022)}" letter-spacing="3">${esc(a.name.toUpperCase())}</text><text x="${width*.07}" y="${height*(portrait?.34:.38)}" fill="${esc(a.accentColor)}" font-family="${bodyFont}" font-weight="700" font-size="${Math.round(width*.022)}">${esc(String(p.transactionType).toUpperCase())} · ${esc(p.propertyType)}</text><foreignObject x="${width*.07}" y="${height*(portrait?.38:.42)}" width="${width*.82}" height="${height*.25}"><div xmlns="http://www.w3.org/1999/xhtml" style="font:700 ${titleSize}px/1.06 ${headingFont};color:white;letter-spacing:0">${esc(p.title)}</div></foreignObject><text x="${width*.07}" y="${height*(portrait?.66:.74)}" fill="#fff" font-family="${headingFont}" font-size="${Math.round(width*.044)}">${esc(price)}</text><text x="${width*.07}" y="${height*(portrait?.72:.82)}" fill="#d5e2dd" font-family="${bodyFont}" font-size="${Math.round(width*.018)}">${esc([p.suburb,p.city].filter(Boolean).join(", ")||p.location)}</text><text x="${width*.07}" y="${height*(portrait?.78:.89)}" fill="#fff" font-family="${bodyFont}" font-size="${Math.round(width*.017)}">${esc(facts.join("  ·  "))}</text><line x1="${width*.07}" y1="${height*.9}" x2="${width*.93}" y2="${height*.9}" stroke="#ffffff55"/><text x="${width*.07}" y="${height*.95}" fill="#d5e2dd" font-family="${bodyFont}" font-size="${Math.round(width*.014)}">${esc(p.reference)} · ${esc(a.phone||a.email||"")}</text></svg>`}
export async function renderMarketingJob(jobId:string,agencyId:string){const job=await env.DB.prepare("SELECT id,format,input_snapshot AS inputSnapshot FROM marketing_render_jobs WHERE id=? AND agency_id=?").bind(jobId,agencyId).first<any>();if(!job)throw new Error("Render job was not found.");const snapshot=JSON.parse(job.inputSnapshot),format=job.format as MarketingFormat,spec=MARKETING_FORMATS[format];if(!spec)throw new Error("Unsupported marketing format.");let bytes:Uint8Array,contentType:string,kind=spec.kind;if(format==="brochure"||format==="flyer"){bytes=await brochurePdf(snapshot,format==="flyer");contentType="application/pdf"}else if(format==="qr"){const svg=await QRCode.toString(snapshot.shareUrl,{type:"svg",errorCorrectionLevel:"M",margin:2,color:{dark:snapshot.agency.primaryColor||"#153b34",light:"#ffffff"}});bytes=new TextEncoder().encode(svg);contentType="image/svg+xml"}else{bytes=new TextEncoder().encode(creativeSvg(snapshot,spec.width,spec.height));contentType="image/svg+xml"}const objectKey=`tenants/${agencyId}/marketing/${jobId}/${kind}`;await bucket().put(objectKey,bytes,{httpMetadata:{contentType},customMetadata:{agencyId,jobId,format}});await env.DB.batch([env.DB.prepare("INSERT INTO marketing_outputs(id,agency_id,job_id,kind,object_key,content_type,byte_size) VALUES(?,?,?,?,?,?,?) ON CONFLICT(job_id,kind) DO UPDATE SET object_key=excluded.object_key,content_type=excluded.content_type,byte_size=excluded.byte_size,created_at=CURRENT_TIMESTAMP").bind(crypto.randomUUID(),agencyId,jobId,kind,objectKey,contentType,bytes.byteLength),env.DB.prepare("UPDATE marketing_render_jobs SET status='complete',completed_at=CURRENT_TIMESTAMP,last_error=NULL WHERE id=? AND agency_id=?").bind(jobId,agencyId)]);return{kind,contentType,byteSize:bytes.byteLength}}
