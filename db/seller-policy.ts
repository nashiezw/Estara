export const hashSellerToken=async(value:string)=>Array.from(new Uint8Array(await crypto.subtle.digest("SHA-256",new TextEncoder().encode(value)))).map(byte=>byte.toString(16).padStart(2,"0")).join("");
export const sellerEmail=(value:unknown)=>String(value??"").trim().toLowerCase();
export const validSellerEmail=(value:string)=>/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(value)&&value.length<=160;
export function sellerMomentum(views:number,enquiries:number,viewings:number){if(viewings>=3||enquiries>=5)return "Strong interest";if(viewings||enquiries>=2||views>=20)return "Building momentum";return "Early activity"}
export function sellerReportCopy(property:string,views:number,enquiries:number,viewings:number){const summary=`${property} recorded ${views} verified listing view${views===1?"":"s"}, ${enquiries} enquir${enquiries===1?"y":"ies"} and ${viewings} viewing${viewings===1?"":"s"} during this reporting period.`;const recommendedAction=viewings>=3?"Follow up with the most interested viewers and prepare for offer conversations.":enquiries>=2?"Contact every active enquirer and convert qualified interest into viewings.":"Continue the current campaign and review presentation, pricing and distribution with the seller.";return{summary,recommendedAction,momentum:sellerMomentum(views,enquiries,viewings)}}
